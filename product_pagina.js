let aantalSterren;
function puntenGeven(item){
aantalSterren = item.id[0];
let numberSter = item.id.substring(1);
for(let i = 0 ; i< 5;i++){
    if(i<aantalSterren){
        document.getElementById('i+1)+ numberSter').style.color= "orange";
    }
}
}